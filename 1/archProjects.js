function archProjects(input) {

    let archName = input[0];
    let projectNum = input[1];
    let hours = input[2];

    console.log(`The architect ${archName} will need ${hours} hours to complete ${projectNum} projects.`);

}

archProjects(["George", 12, 4])